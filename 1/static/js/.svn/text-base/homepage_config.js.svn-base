$(document).ready(function() {
    (function() {
        window.onresize = arguments.callee;
        var navbarHeight = 45;
        h = $(window).height() - navbarHeight;
        w = $(window).width() - $("picshow").outerWidth();
        $('#map').height(h);
        $('#map').width(w);
        $('#container1').height(h);
    })();
});
